/* all generic sizes */
const size = {
  mobile: "500px",
  tablet: "768px",
  browser: "950px",
}

export const device = {
  mobile: `(min-width: ${size.mobile})`,
  tablet: `(min-width: ${size.tablet})`,
  browser: `(min-width: ${size.browser})`,
}
